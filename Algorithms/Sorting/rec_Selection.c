#include <stdio.h>
int index = 0;
void selectionR(int arr[], int n)
{
}

int main(int argc, char const *argv[])
{
    int arr[] = {3, 45, 67, 12, 34, 2};
    int n = sizeof(arr) / sizeof(arr[0]);
    selectionR(arr, n);
    return 0;
}
